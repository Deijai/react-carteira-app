import styled from "styled-components";

export const Container = styled.div`

    > select {
        padding: 7px 10px;
        border: 0;
        outline: none;
        border-radius: 3px;
        margin-left: 7px;
    }

`;
